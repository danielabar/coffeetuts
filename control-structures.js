// Generated by CoffeeScript 1.7.1
(function() {
  var firstName, giveWork, giveWork2, greet, greet2, lastName, machine, machine2, name, person, person1, person2;

  name = "Jeremy";

  if (name === "Jeremy") {
    console.log("Jeremy");
  }

  name = "Jim";

  if (name === "Jeremy") {
    console.log("Jeremy");
  } else {
    console.log("not Jeremy");
  }

  name = "Sara";

  if (name === "Jeremy") {
    console.log("Jeremy");
  } else if (name === "Sara") {
    console.log("Sara");
  } else {
    console.log("not Jeremy or Sara");
  }

  firstName = "Tyler";

  if (firstName === "Tyler") {
    console.log("Hi Tyler");
  }

  if (name === "Jeremy") {
    console.log("Jeremy");
  } else if (name === "Sara") {
    console.log("Sara");
  } else {
    console.log("not Jeremy or Sara");
  }

  lastName = "Smith";

  if (lastName === "Smith") {
    console.log(lastName);
  }

  machine = {
    running: false,
    turnOn: function() {
      return this.running = true;
    }
  };

  if (!machine.running) {
    machine.turnOn();
  }

  if (!machine.running) {
    machine.turnOn();
  }

  if (!machine.running) {
    machine.turnOn();
  }

  if (!machine.running) {
    machine.turnOn();
  }

  console.log(machine.running);

  machine2 = {
    running: false,
    turnOn: function() {
      return this.running = true;
    },
    turnOff: function() {
      return this.running = false;
    }
  };

  if (!machine2.running) {
    machine2.turnOn();
  } else {
    machine2.turnOff;
  }

  person = {
    name: "Jeremy",
    job: "Programmer"
  };

  giveWork = function(person) {
    switch (person.job) {
      case "Programmer":
        return console.log("Coding task ready for " + person.name);
      case "Designer":
        return console.log("Design task ready for " + person.name);
      default:
        return console.log("Ummm... do you work here?");
    }
  };

  giveWork2 = function(person) {
    switch (person.job) {
      case "Programmer":
        return console.log("Coding task ready for " + person.name);
      case "Designer":
        return console.log("Design task ready for " + person.name);
      default:
        return console.log("Ummm... do you work here?");
    }
  };

  giveWork2(person);

  person1 = {
    name: "Jeremy",
    relationship: "Friend"
  };

  person2 = {
    name: "Jim",
    relationship: "Boss"
  };

  greet = function(person) {
    return console.log(person.relationship === "Friend" ? "Hi, " + person.name : person.relationship === "Boss" ? "Hello, sir" : void 0);
  };

  greet(person1);

  greet(person2);

  console.log("===========");

  greet2 = function(person) {
    var msg;
    msg = (function() {
      switch (person.relationship) {
        case "Friend":
          return "Hi, " + person.name;
        case "Boss":
          return "Hello sir";
        default:
          return "Who are you?";
      }
    })();
    return console.log(msg);
  };

  greet2(person1);

  greet2(person2);

}).call(this);
