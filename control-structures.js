// Generated by CoffeeScript 1.7.1
(function() {
  var firstName, giveWork, lastName, machine, machine2, name, person;

  name = "Jeremy";

  if (name === "Jeremy") {
    console.log("Jeremy");
  }

  name = "Jim";

  if (name === "Jeremy") {
    console.log("Jeremy");
  } else {
    console.log("not Jeremy");
  }

  name = "Sara";

  if (name === "Jeremy") {
    console.log("Jeremy");
  } else if (name === "Sara") {
    console.log("Sara");
  } else {
    console.log("not Jeremy or Sara");
  }

  firstName = "Tyler";

  if (firstName === "Tyler") {
    console.log("Hi Tyler");
  }

  if (name === "Jeremy") {
    console.log("Jeremy");
  } else if (name === "Sara") {
    console.log("Sara");
  } else {
    console.log("not Jeremy or Sara");
  }

  lastName = "Smith";

  if (lastName === "Smith") {
    console.log(lastName);
  }

  machine = {
    running: false,
    turnOn: function() {
      return this.running = true;
    }
  };

  if (!machine.running) {
    machine.turnOn();
  }

  if (!machine.running) {
    machine.turnOn();
  }

  if (!machine.running) {
    machine.turnOn();
  }

  if (!machine.running) {
    machine.turnOn();
  }

  console.log(machine.running);

  machine2 = {
    running: false,
    turnOn: function() {
      return this.running = true;
    },
    turnOff: function() {
      return this.running = false;
    }
  };

  if (!machine2.running) {
    machine2.turnOn();
  } else {
    machine2.turnOff;
  }

  person = {
    name: "Jeremy",
    job: "Programmer"
  };

  giveWork = function(person) {
    switch (person.job) {
      case "Programmer":
        return console.log("Coding task ready for " + person.name);
      case "Designer":
        return console.log("Design task ready for " + person.name);
      default:
        return console.log("Ummm... do you work here?");
    }
  };

}).call(this);
